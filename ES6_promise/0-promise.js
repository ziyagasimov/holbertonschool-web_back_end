export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    /* * For this basic task, we just need to return the instance.
     * We can immediately resolve it or leave it pending.
     */
    resolve();
  });
}
